- name: print directory info
  shell: |
    pwd
    ls -a

- name: Copy index test page
  template:
    src: "dist"
    dest: "~/web/"

- name: Executing node
  shell: |
    pm2 start ~/web/index.js -f